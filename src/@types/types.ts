export type CardType = {
    id: string
    oldPrice: string
    price: string
    title: string
    seen: boolean
    locality: string
    date: number
}

export type ImageType = {
    download_url: string
}